services:
  app:
    build: ./app
    ports:
      - "3000:3000"
    restart: always
    container_name: chaos_app
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 5s
